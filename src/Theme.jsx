import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    type: "light",
    primary: {
      main: "#ff4500",
    },
    secondary: {
      main: "#ff4500",
    },
    background: {
      default: "#ffffff",
    },
  },
});
